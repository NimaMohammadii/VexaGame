// Chicken Cross owns its scene, controls and client state. Mounted only through the Play Hub lazy section.
export const CHICKEN_CROSS_SECTION = String.raw`
<section id="hilo" class="view cc-view" aria-label="Chicken Cross">
  <style>
    body:has(#hilo.active) .tabs{display:none!important}body:has(#hilo.active) .app,body:has(#hilo.active) .content,body:has(#hilo.active) header.top{background:#090609!important}body:has(#hilo.active) .app{display:flex;flex-direction:column;padding:0 0 env(safe-area-inset-bottom)}body:has(#hilo.active) header.top{flex-shrink:0;margin:0 16px}body:has(#hilo.active) [data-lazy-section-host="hilo"]{width:100%;flex:1;min-height:0;overflow:hidden}
    .cc-view{--line:rgba(211,145,158,.18);width:100%;height:100%;min-height:0;overflow:hidden!important;color:#f5f1ef;background:#090609!important;padding:0;box-sizing:border-box}.cc-page{width:100%;max-width:none;height:100%;min-height:0;margin:0;display:grid;grid-template-rows:minmax(190px,1fr) auto}.cc-stage{min-height:190px;position:relative;overflow:hidden;background:#160d12;isolation:isolate}.cc-scene{position:absolute;inset:0;overflow:hidden;background:#130b11}.cc-world{position:absolute;inset:0 auto 0 0;height:100%;display:flex;will-change:transform;transform:translate3d(0,0,0)}.cc-road-piece{height:100%;width:auto;max-width:none;display:block;flex:none;user-select:none;-webkit-user-drag:none;pointer-events:none}.cc-entities{position:absolute;inset:0;pointer-events:none}.cc-chicken,.cc-car,.cc-barrier{position:absolute;object-fit:contain;filter:drop-shadow(0 12px 10px rgba(0,0,0,.44));will-change:transform}.cc-chicken{z-index:4;width:clamp(58px,13vh,116px);height:clamp(58px,13vh,116px);transition:left .33s cubic-bezier(.22,.75,.2,1),top .33s cubic-bezier(.22,.75,.2,1);transform:translate(-50%,-50%)}.cc-chicken.hop{animation:cc-hop .33s cubic-bezier(.22,.75,.2,1)}.cc-chicken.hit{animation:cc-hit .26s ease-in-out}.cc-car{z-index:3;width:clamp(86px,19vh,184px);height:clamp(58px,12vh,120px);opacity:.92;animation:cc-drive 1.15s linear infinite}.cc-car:nth-child(2){animation-duration:1.36s;animation-delay:-.48s}.cc-car:nth-child(3){animation-duration:1.02s;animation-delay:-.71s}.cc-barrier{z-index:5;width:clamp(52px,12vh,108px);height:clamp(34px,8vh,76px);opacity:0;transform:translate(-50%,-50%) scale(.72);transition:opacity .2s,transform .24s}.cc-barrier.active{opacity:1;transform:translate(-50%,-50%) scale(1)}.cc-stage:after{content:"";position:absolute;inset:0;pointer-events:none;background:linear-gradient(180deg,rgba(15,4,10,.23),transparent 23%,transparent 70%,rgba(8,4,8,.88) 100%);z-index:8}@keyframes cc-hop{0%,100%{transform:translate(-50%,-50%)}50%{transform:translate(-50%,calc(-50% - 20px)) rotate(-4deg)}}@keyframes cc-hit{0%,100%{transform:translate(-50%,-50%)}45%{transform:translate(calc(-50% + 18px),calc(-50% - 7px)) rotate(18deg)}}@keyframes cc-drive{0%{transform:translate3d(-10px,0,0)}100%{transform:translate3d(46px,0,0)}}
    .cc-topline{position:absolute;z-index:4;top:20px;left:20px;right:20px;display:flex;align-items:flex-start;justify-content:space-between;pointer-events:none;text-shadow:0 2px 12px #000}.cc-multiplier{font:750 42px/1 system-ui,sans-serif;letter-spacing:-.055em;font-variant-numeric:tabular-nums}.cc-multiplier small{display:block;margin-top:7px;font:700 10px system-ui,sans-serif;letter-spacing:.13em;color:#d3bfc4;text-transform:uppercase}.cc-counter{padding:8px 12px;border:1px solid rgba(202,108,124,.38);border-radius:100px;background:rgba(38,12,24,.82);font:700 11px system-ui,sans-serif;letter-spacing:.12em}.cc-status{position:absolute;z-index:4;left:18px;right:18px;bottom:24px;min-height:19px;text-align:center;font:650 13px system-ui,sans-serif;color:#f0e9e8;text-shadow:0 2px 9px #000}.cc-status.win{color:#b8efd0}.cc-status.lose{color:#ffadb6}.cc-loading{position:absolute;inset:0;z-index:5;display:grid;place-content:center;text-align:center;gap:9px;background:#160b12;color:#d7c9cc;font:650 12px system-ui,sans-serif;letter-spacing:.1em;transition:opacity .35s,visibility .35s}.cc-loading.ready{opacity:0;visibility:hidden;pointer-events:none}.cc-loading b{font-size:18px;letter-spacing:0;color:#fff}
    .cc-panel{position:relative;z-index:3;padding:4px 18px 10px;background:#090609}.cc-panel-inner{border:1px solid var(--line);border-radius:21px;background:linear-gradient(160deg,#281119,#170d13 65%,#10080e);box-shadow:inset 0 1px rgba(255,255,255,.06),0 14px 26px rgba(0,0,0,.22);padding:17px}.cc-controls-label{display:flex;justify-content:space-between;color:#c5aeb4;text-transform:uppercase;letter-spacing:.11em;font:700 10px system-ui,sans-serif;margin-bottom:10px}.cc-difficulties{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}.cc-difficulties button{height:42px;border-radius:11px;border:1px solid var(--line);background:#2b1720;color:#decfd1;font:700 12px system-ui,sans-serif;transition:background .2s,transform .2s}.cc-difficulties button.active{background:#743344;color:#fff;border-color:#bd6678}.cc-difficulties button:active,.cc-actions button:active{transform:scale(.98)}.cc-wager{display:grid;grid-template-columns:44px 1fr 44px;align-items:center;gap:8px;margin-bottom:14px}.cc-wager button,.cc-wager input{height:46px;border:1px solid var(--line);border-radius:12px;background:#2b1720;color:#f6f0ef;font:750 17px system-ui,sans-serif;text-align:center;box-sizing:border-box;min-width:0}.cc-wager input{outline:none;font-variant-numeric:tabular-nums}.cc-wager input:focus{border-color:#bd7180}.cc-wager button{font-size:22px}.cc-unit{position:relative}.cc-unit input{width:100%;padding:0 64px 0 15px}.cc-unit span{position:absolute;right:12px;top:16px;color:#d0b6bc;font:700 11px system-ui,sans-serif;pointer-events:none}.cc-decision{display:flex;align-items:center;justify-content:space-between;gap:8px;margin:0 0 12px;color:#d0b7bd;font:650 11px system-ui,sans-serif}.cc-decision strong{color:#f8f3f1;font:750 14px system-ui,sans-serif;font-variant-numeric:tabular-nums;text-align:right}.cc-panel button,.cc-panel input{touch-action:manipulation}.cc-panel button{user-select:none;-webkit-user-select:none}.cc-actions{display:grid;grid-template-columns:1.25fr .75fr;gap:9px}.cc-actions button{height:52px;border:0;border-radius:13px;font:750 15px system-ui,sans-serif;transition:transform .12s,opacity .12s}.cc-go{background:linear-gradient(160deg,#a33652,#5d182c);color:#fff;border:1px solid #c65872!important;box-shadow:inset 0 1px rgba(255,255,255,.15)}.cc-cash{display:block;background:#263c35;color:#d4f7e5;border:1px solid #466858!important}.cc-actions button:disabled{opacity:.5}@media(max-height:700px){.cc-panel-inner{padding:13px}.cc-difficulties{margin-bottom:10px}}@media(prefers-reduced-motion:reduce){.cc-difficulties button,.cc-actions button{transition:none}}
  </style>
  <div class="cc-page"><div class="cc-stage" data-cc-stage><div class="cc-scene" data-cc-scene></div><div class="cc-loading" data-cc-loading><b>CHICKEN CROSS</b><span>Preparing the road…</span></div><div class="cc-topline"><div class="cc-multiplier" data-cc-multiplier>1.00×<small data-cc-next>Next crossing</small></div><div class="cc-counter" data-cc-counter>0 / 8</div></div><div class="cc-status" data-cc-status aria-live="polite">Choose a risk and start crossing</div></div><div class="cc-panel"><div class="cc-panel-inner"><div class="cc-setup" data-cc-setup><div class="cc-controls-label"><span>Difficulty</span><span>8 lanes</span></div><div class="cc-difficulties"><button type="button" data-cc-risk="easy" class="active">Easy</button><button type="button" data-cc-risk="medium">Medium</button><button type="button" data-cc-risk="hard">Hard</button></div><div class="cc-controls-label"><span>Bet amount</span><span>GRAM</span></div><div class="cc-wager"><button type="button" data-cc-half aria-label="Halve bet">−</button><div class="cc-unit"><input data-cc-bet type="text" inputmode="decimal" value="0.1" aria-label="Bet amount in GRAM"><span>GRAM</span></div><button type="button" data-cc-double aria-label="Double bet">+</button></div></div><div class="cc-decision"><span>Cash out now</span><strong data-cc-quote>Cross one lane first</strong></div><div class="cc-actions"><button type="button" class="cc-go" data-cc-go>Start crossing</button><button type="button" class="cc-cash" data-cc-cash>Cash out</button></div></div></div></div>
  <script type="module">
  (async function(){
    const root=document.getElementById('hilo');if(!root||root.dataset.ready)return;root.dataset.ready='1';const q=s=>root.querySelector(s),stage=q('[data-cc-stage]'),scene=q('[data-cc-scene]'),loading=q('[data-cc-loading]'),go=q('[data-cc-go]'),cash=q('[data-cc-cash]'),input=q('[data-cc-bet]'),status=q('[data-cc-status]'),quote=q('[data-cc-quote]');let mode='easy',round=null,busy=false,pendingAction='',engine=null,active=false,requestVersion=0;
    function tgData(){return String(window.Telegram&&window.Telegram.WebApp&&window.Telegram.WebApp.initData||'')}function money(n){return(Math.round(n*10000)/10000).toFixed(4).replace(/0+$/,'').replace(/\.$/,'')}function haptic(kind){try{const h=window.Telegram.WebApp.HapticFeedback;if(kind==='success'||kind==='error')h.notificationOccurred(kind);else h.impactOccurred('light')}catch(e){}}function message(text,kind){status.textContent=text;status.className='cc-status'+(kind?' '+kind:'')}function betNano(){return Math.floor(Number(String(input.value).replace(',','.'))*1e9)}function syncBalance(n){if(window.VexaTonBalance&&Number.isFinite(Number(n)))window.VexaTonBalance.write(Math.max(0,Math.floor(Number(n))),0)}
    async function api(path,body){const initData=tgData();if(!initData)throw Error('Open the Mini App in Telegram');const options=body?{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.assign({initData},body))}:{headers:{'x-telegram-init-data':initData},cache:'no-store'};const response=await fetch('/app/api/chicken-cross/'+path,options),data=await response.json().catch(()=>null);if(!response.ok)throw Error(data&&data.error||'Could not reach the game server');return data}
    function render(){const playing=round&&round.status==='active';go.textContent=pendingAction==='start'?'Starting…':pendingAction==='cross'?'Crossing…':playing?'Cross next lane':'Start crossing';cash.textContent=pendingAction==='cashout'?'Cashing out…':'Cash out';go.disabled=busy||!engine;cash.disabled=busy||!engine||!playing||round.step<1;quote.textContent=playing&&round.step>0&&Number.isSafeInteger(round.cashoutQuoteNano)?'≈ '+money(round.cashoutQuoteNano/1e9)+' GRAM':'Cross one lane first';input.disabled=busy||!!playing;q('[data-cc-half]').disabled=busy||!!playing;q('[data-cc-double]').disabled=busy||!!playing;root.querySelectorAll('[data-cc-risk]').forEach(b=>{b.classList.toggle('active',b.dataset.ccRisk===mode);b.disabled=busy||!!playing});q('[data-cc-multiplier]').firstChild.nodeValue=round&&round.status==='lost'?'0.00×':(round?Number(round.multiplier):1).toFixed(2)+'×';q('[data-cc-next]').textContent=playing?'NEXT '+Number(round.nextMultiplier).toFixed(2)+'×':round&&round.status==='lost'?'ROUND ENDED':round&&round.status==='cashed'?'CASHED OUT':'NEXT CROSSING';q('[data-cc-counter]').textContent=(round?round.step:0)+' / 8'}
    function apply(data){if(data&&data.round){round=data.round;mode=round.difficulty;input.value=money(round.amountNano/1e9)}if(data&&data.tonBalanceNano!==undefined)syncBalance(data.tonBalanceNano);render()}function setBusy(v,a=''){busy=v;pendingAction=v?a:'';render()}
    async function start(){if(busy)return;const amount=betNano();if(!Number.isSafeInteger(amount)||amount<1000000||amount>20000000000){message('Bet must be between 0.001 and 20 GRAM','lose');return}setBusy(true,'start');message('Starting round…');haptic('light');try{const data=await api('start',{amountNano:amount,difficulty:mode});apply(data);engine&&engine.setStep(round.step,false);message('Each crossing risks the round · choose your next step')}catch(e){message(e.message,'lose')}finally{setBusy(false)}}
    async function cross(){if(busy||!round||round.status!=='active')return;setBusy(true,'cross');const previous=round.step,movement=engine?engine.move(previous+1):Promise.resolve();message('Crossing…');haptic('light');try{const data=await api('step',{roundId:round.id});await movement;if(data.event==='sync'){apply(data);engine&&engine.setStep(round.step,false)}else if(data.event==='hit'){if(engine)await engine.hit(data.round.step);apply(data);engine&&engine.setStep(0,false)}else{apply(data);engine&&engine.confirmStep(data.round.step)}if(data.event==='hit'){message('Hit by traffic · round ended','lose');haptic('error')}else if(data.event==='finish'){message('All lanes crossed · payout credited','win');haptic('success')}else if(data.event==='safe')message('Safe · cash out or cross again');else message('Round updated')}catch(e){message(e.message,'lose');await restore()}finally{setBusy(false)}}
    async function cashOut(){if(busy||!round)return;setBusy(true,'cashout');message('Cashing out…');haptic('light');try{const data=await api('cashout',{roundId:round.id});apply(data);if(round.status==='cashed'){engine&&engine.setStep(0,false);message('Cashed out '+money(round.payoutNano/1e9)+' GRAM','win');haptic('success')}else message('Round updated · choose your next move')}catch(e){message(e.message,'lose');await restore()}finally{setBusy(false)}}
    async function restore(){if(!tgData())return;const version=++requestVersion;try{const data=await api('state');if(version!==requestVersion)return;round=data.round||null;if(round)apply(data);else render();engine&&engine.setStep(round&&round.status==='active'?round.step:0,false);if(round&&round.status==='active')message('Round restored · cross or cash out')}catch(e){message(e.message,'lose')}}
    root.querySelectorAll('[data-cc-risk]').forEach(b=>b.addEventListener('click',()=>{if(busy||round&&round.status==='active')return;mode=b.dataset.ccRisk;render();haptic('light')}));q('[data-cc-half]').addEventListener('click',()=>{input.value=money(Math.max(.001,(Number(input.value)||.1)/2))});q('[data-cc-double]').addEventListener('click',()=>{input.value=money(Math.min(20,(Number(input.value)||.1)*2))});go.addEventListener('click',()=>round&&round.status==='active'?cross():start());cash.addEventListener('click',cashOut);
    function loadImage(src){return new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(Error('Could not load the road image'));image.src=src})}async function activate(){if(active)return;active=true;const version=++requestVersion;try{if(!engine){const images=await Promise.all(['/assets/chicken-cross/road-space-v1.jpeg'].map(loadImage));if(!active||version!==requestVersion)return;engine=createRoad(scene,images);loading.classList.add('ready');render()}engine.setActive(!document.hidden);await restore()}catch(e){loading.innerHTML='<b>Could not load the road</b><span>Try opening the game again</span>';message('Road rendering unavailable','lose')}}function deactivate(){active=false;requestVersion++;if(engine)engine.setActive(false)}window.addEventListener('vexa:view-changed',event=>{if(event.detail&&event.detail.id==='hilo')activate();else deactivate()});document.addEventListener('visibilitychange',()=>{if(engine)engine.setActive(active&&!document.hidden)});if(root.classList.contains('active'))activate();render();
    function createRoad(container,images){const world=document.createElement('div');world.className='cc-world';images.forEach((source,index)=>{const piece=document.createElement('img');piece.className='cc-road-piece';piece.alt='';piece.decoding='async';piece.draggable=false;piece.src=source.src;piece.dataset.panel=String(index+1);world.appendChild(piece)});container.appendChild(world);let isActive=false,raf=0,worldWidth=1,viewportWidth=1,shown=0,target=0,camera=0,moveFrom=0,moveStart=0,moveDone=null,hitStart=0,hitDone=null,last=0;const clamp=(n,min,max)=>Math.max(min,Math.min(max,n)),ease=t=>1-Math.pow(1-t,3),roadStart=.2205,roadEnd=.843;function position(step){return step<=0?roadStart-.06:roadStart+(roadEnd-roadStart)*clamp(step,0,8)/8}function wantedCamera(){return clamp(position(shown)*worldWidth-viewportWidth*.31,0,Math.max(0,worldWidth-viewportWidth))}function renderRoad(){world.style.transform='translate3d('+-camera.toFixed(2)+'px,0,0)'}function resize(){viewportWidth=Math.max(1,container.clientWidth);worldWidth=0;Array.from(world.children).forEach((piece,index)=>{const image=images[index],width=image.naturalWidth/image.naturalHeight*Math.max(1,container.clientHeight);piece.style.width=width+'px';worldWidth+=width});world.style.width=worldWidth+'px';camera=wantedCamera();renderRoad()}const observer=new ResizeObserver(resize);observer.observe(container);resize();function doneMove(){if(moveDone){const done=moveDone;moveDone=null;done()}}function doneHit(){if(hitDone){const done=hitDone;hitDone=null;done()}}function requestFrame(){if(isActive&&!raf){last=performance.now();raf=requestAnimationFrame(frame)}}function frame(now){raf=0;if(!isActive)return;const dt=Math.min(.05,(now-last)/1000||0);last=now;let keep=false;if(moveStart){const t=clamp((now-moveStart)/330,0,1);shown=moveFrom+(target-moveFrom)*ease(t);if(t===1){shown=target;moveStart=0;doneMove()}else keep=true}if(hitStart){const t=clamp((now-hitStart)/260,0,1);container.style.filter='brightness('+(1-.18*Math.sin(t*Math.PI))+')';if(t===1){hitStart=0;container.style.filter='';doneHit()}else keep=true}const wanted=wantedCamera();camera+=(wanted-camera)*Math.min(1,dt*13);if(Math.abs(wanted-camera)>.25)keep=true;else camera=wanted;renderRoad();if(keep)raf=requestAnimationFrame(frame)}function setActive(value){isActive=value;if(!value){if(raf)cancelAnimationFrame(raf);raf=0;shown=target;camera=wantedCamera();renderRoad();doneMove();doneHit()}else{resize();requestFrame()}}function setStep(step){shown=target=clamp(Number(step)||0,0,8);camera=wantedCamera();renderRoad();doneMove()}function move(step){moveFrom=shown;target=clamp(Number(step)||0,0,8);moveStart=performance.now();requestFrame();return new Promise(resolve=>{moveDone=resolve})}function hit(){hitStart=performance.now();requestFrame();return new Promise(resolve=>{hitDone=resolve})}function confirmStep(step){target=shown=clamp(Number(step)||0,0,8);requestFrame()}return{setActive,setStep,move,hit,confirmStep}}
  })();
  </script>
</section>`;
