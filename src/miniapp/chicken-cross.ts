// Chicken Cross owns its scene, controls and client state. Mounted only through the Play Hub lazy section.
export const CHICKEN_CROSS_SECTION = String.raw`
<section id="hilo" class="view cc-view" aria-label="Chicken Cross">
  <style>
    body:has(#hilo.active) .tabs{display:none!important}body:has(#hilo.active) .app,body:has(#hilo.active) .content,body:has(#hilo.active) header.top{background:#000!important}body:has(#hilo.active) .app{display:flex;flex-direction:column;padding:0 0 env(safe-area-inset-bottom)}body:has(#hilo.active) header.top{flex-shrink:0;margin:0 16px}body:has(#hilo.active) [data-lazy-section-host="hilo"]{width:100%;flex:1;min-height:0;overflow:hidden}
    .cc-view{--line:rgba(211,145,158,.18);width:100%;height:100%;min-height:0;overflow:hidden!important;color:#f5f1ef;background:#000!important;padding:0;box-sizing:border-box}.cc-page{width:100%;max-width:none;height:100%;min-height:0;margin:0;display:grid;grid-template-rows:minmax(190px,512px) auto;align-content:start}.cc-stage{min-height:190px;position:relative;overflow:hidden;background:#000;isolation:isolate}.cc-scene{position:absolute;inset:0;overflow:hidden;background:#000}.cc-world{position:absolute;inset:0 auto 0 0;height:100%;will-change:transform;transform:translate3d(0,0,0)}.cc-road-piece{height:100%!important;width:auto!important;max-width:none;display:block;user-select:none;-webkit-user-drag:none;pointer-events:none}.cc-entities{position:absolute;inset:0;pointer-events:none}.cc-chicken{position:absolute;z-index:4;width:clamp(54px,12vh,102px);height:clamp(54px,12vh,102px);object-fit:contain;filter:drop-shadow(0 10px 8px rgba(0,0,0,.5));will-change:transform,left;transition:left .22s cubic-bezier(.22,.75,.2,1);transform:translate(-50%,-50%)}.cc-chicken.hop{animation:cc-hop .26s cubic-bezier(.22,.75,.2,1)}.cc-chicken.hit{animation:cc-hit .26s ease-in-out}.cc-stage:after{content:"";position:absolute;inset:0;pointer-events:none;background:linear-gradient(180deg,rgba(0,0,0,.12),transparent 30%,rgba(0,0,0,.32) 100%);z-index:8}@keyframes cc-hop{0%,100%{transform:translate(-50%,-50%)}50%{transform:translate(-50%,calc(-50% - 18px)) rotate(-4deg)}}@keyframes cc-hit{0%,100%{transform:translate(-50%,-50%)}45%{transform:translate(calc(-50% + 18px),calc(-50% - 7px)) rotate(18deg)}}
    .cc-loading{position:absolute;inset:0;z-index:9;display:grid;place-content:center;text-align:center;gap:9px;background:#000;color:#d7c9cc;font:650 12px system-ui,sans-serif;letter-spacing:.1em;transition:opacity .35s,visibility .35s}.cc-loading.ready{opacity:0;visibility:hidden;pointer-events:none}.cc-loading b{font-size:18px;letter-spacing:0;color:#fff}
    .cc-panel{position:relative;z-index:3;padding:4px 18px 10px;background:#090609}.cc-panel-inner{border:1px solid var(--line);border-radius:21px;background:linear-gradient(160deg,#281119,#170d13 65%,#10080e);box-shadow:inset 0 1px rgba(255,255,255,.06),0 14px 26px rgba(0,0,0,.22);padding:17px}.cc-controls-label{display:flex;justify-content:space-between;color:#c5aeb4;text-transform:uppercase;letter-spacing:.11em;font:700 10px system-ui,sans-serif;margin-bottom:10px}.cc-difficulties{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}.cc-difficulties button{height:42px;border-radius:11px;border:1px solid var(--line);background:#2b1720;color:#decfd1;font:700 12px system-ui,sans-serif;transition:background .2s,transform .2s}.cc-difficulties button.active{background:#743344;color:#fff;border-color:#bd6678}.cc-difficulties button:active,.cc-actions button:active{transform:scale(.98)}.cc-wager{display:grid;grid-template-columns:44px 1fr 44px;align-items:center;gap:8px;margin-bottom:14px}.cc-wager button,.cc-wager input{height:46px;border:1px solid var(--line);border-radius:12px;background:#2b1720;color:#f6f0ef;font:750 17px system-ui,sans-serif;text-align:center;box-sizing:border-box;min-width:0}.cc-wager input{outline:none;font-variant-numeric:tabular-nums}.cc-wager input:focus{border-color:#bd7180}.cc-wager button{font-size:22px}.cc-unit{position:relative}.cc-unit input{width:100%;padding:0 64px 0 15px}.cc-unit span{position:absolute;right:12px;top:16px;color:#d0b6bc;font:700 11px system-ui,sans-serif;pointer-events:none}.cc-decision{display:flex;align-items:center;justify-content:space-between;gap:8px;margin:0 0 12px;color:#d0b7bd;font:650 11px system-ui,sans-serif}.cc-decision strong{color:#f8f3f1;font:750 14px system-ui,sans-serif;font-variant-numeric:tabular-nums;text-align:right}.cc-panel button,.cc-panel input{touch-action:manipulation}.cc-panel button{user-select:none;-webkit-user-select:none}.cc-actions{display:grid;grid-template-columns:1.25fr .75fr;gap:9px}.cc-actions button{height:52px;border:0;border-radius:13px;font:750 15px system-ui,sans-serif;transition:transform .12s,opacity .12s}.cc-go{background:linear-gradient(160deg,#a33652,#5d182c);color:#fff;border:1px solid #c65872!important;box-shadow:inset 0 1px rgba(255,255,255,.15)}.cc-cash{display:block;background:#263c35;color:#d4f7e5;border:1px solid #466858!important}.cc-actions button:disabled{opacity:.5}@media(max-height:700px){.cc-panel-inner{padding:13px}.cc-difficulties{margin-bottom:10px}}@media(prefers-reduced-motion:reduce){.cc-difficulties button,.cc-actions button{transition:none}}
  </style>
  <div class="cc-page"><div class="cc-stage" data-cc-stage><div class="cc-scene" data-cc-scene></div><div class="cc-loading" data-cc-loading><b>CHICKEN CROSS</b><span>Preparing the road…</span></div></div><div class="cc-panel"><div class="cc-panel-inner"><div class="cc-setup" data-cc-setup><div class="cc-controls-label"><span>Difficulty</span><span>8 lanes</span></div><div class="cc-difficulties"><button type="button" data-cc-risk="easy" class="active">Easy</button><button type="button" data-cc-risk="medium">Medium</button><button type="button" data-cc-risk="hard">Hard</button></div><div class="cc-controls-label"><span>Bet amount</span><span>GRAM</span></div><div class="cc-wager"><button type="button" data-cc-half aria-label="Halve bet">−</button><div class="cc-unit"><input data-cc-bet type="text" inputmode="decimal" value="0.1" aria-label="Bet amount in GRAM"><span>GRAM</span></div><button type="button" data-cc-double aria-label="Double bet">+</button></div></div><div class="cc-decision"><span>Cash out now</span><strong data-cc-quote>Cross one lane first</strong></div><div class="cc-actions"><button type="button" class="cc-go" data-cc-go>Start crossing</button><button type="button" class="cc-cash" data-cc-cash>Cash out</button></div></div></div></div>
  <script type="module">
  (async function(){
    const root=document.getElementById('hilo');if(!root||root.dataset.ready)return;root.dataset.ready='1';const q=s=>root.querySelector(s),stage=q('[data-cc-stage]'),scene=q('[data-cc-scene]'),loading=q('[data-cc-loading]'),go=q('[data-cc-go]'),cash=q('[data-cc-cash]'),input=q('[data-cc-bet]'),quote=q('[data-cc-quote]');let mode='easy',round=null,busy=false,pendingAction='',engine=null,active=false,requestVersion=0;
    function tgData(){return String(window.Telegram&&window.Telegram.WebApp&&window.Telegram.WebApp.initData||'')}function money(n){return(Math.round(n*10000)/10000).toFixed(4).replace(/0+$/,'').replace(/\.$/,'')}function haptic(kind){try{const h=window.Telegram.WebApp.HapticFeedback;if(kind==='success'||kind==='error')h.notificationOccurred(kind);else h.impactOccurred('light')}catch(e){}}function message(){}function betNano(){return Math.floor(Number(String(input.value).replace(',','.'))*1e9)}function syncBalance(n){if(window.VexaTonBalance&&Number.isFinite(Number(n)))window.VexaTonBalance.write(Math.max(0,Math.floor(Number(n))),0)}
    async function api(path,body){const initData=tgData();if(!initData)throw Error('Open the Mini App in Telegram');const options=body?{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.assign({initData},body))}:{headers:{'x-telegram-init-data':initData},cache:'no-store'};const response=await fetch('/app/api/chicken-cross/'+path,options),data=await response.json().catch(()=>null);if(!response.ok)throw Error(data&&data.error||'Could not reach the game server');return data}
    function render(){const playing=round&&round.status==='active';go.textContent=pendingAction==='start'?'Starting…':pendingAction==='cross'?'Crossing…':playing?'Cross next lane':'Start crossing';cash.textContent=pendingAction==='cashout'?'Cashing out…':'Cash out';go.disabled=busy||!engine;cash.disabled=busy||!engine||!playing||round.step<1;quote.textContent=playing&&round.step>0&&Number.isSafeInteger(round.cashoutQuoteNano)?'≈ '+money(round.cashoutQuoteNano/1e9)+' GRAM':'Cross one lane first';input.disabled=busy||!!playing;q('[data-cc-half]').disabled=busy||!!playing;q('[data-cc-double]').disabled=busy||!!playing;root.querySelectorAll('[data-cc-risk]').forEach(b=>{b.classList.toggle('active',b.dataset.ccRisk===mode);b.disabled=busy||!!playing})}
    function apply(data){if(data&&data.round){round=data.round;mode=round.difficulty;input.value=money(round.amountNano/1e9)}if(data&&data.tonBalanceNano!==undefined)syncBalance(data.tonBalanceNano);render()}function setBusy(v,a=''){busy=v;pendingAction=v?a:'';render()}
    async function start(){if(busy)return;const amount=betNano();if(!Number.isSafeInteger(amount)||amount<1000000||amount>20000000000){message('Bet must be between 0.001 and 20 GRAM','lose');return}setBusy(true,'start');message('Starting round…');haptic('light');try{const data=await api('start',{amountNano:amount,difficulty:mode});apply(data);engine&&engine.setStep(round.step,false);message('Each crossing risks the round · choose your next step')}catch(e){message(e.message,'lose')}finally{setBusy(false)}}
    async function cross(){if(busy||!round||round.status!=='active')return;setBusy(true,'cross');const previous=round.step,movement=engine?engine.move(previous+1):Promise.resolve();message('Crossing…');haptic('light');try{const data=await api('step',{roundId:round.id});await movement;if(data.event==='sync'){apply(data);engine&&engine.setStep(round.step,false)}else if(data.event==='hit'){if(engine)await engine.hit(data.round.step);apply(data);engine&&engine.setStep(0,false)}else{apply(data);engine&&engine.confirmStep(data.round.step)}if(data.event==='hit'){message('Hit by traffic · round ended','lose');haptic('error')}else if(data.event==='finish'){message('All lanes crossed · payout credited','win');haptic('success')}else if(data.event==='safe')message('Safe · cash out or cross again');else message('Round updated')}catch(e){message(e.message,'lose');await restore()}finally{setBusy(false)}}
    async function cashOut(){if(busy||!round)return;setBusy(true,'cashout');message('Cashing out…');haptic('light');try{const data=await api('cashout',{roundId:round.id});apply(data);if(round.status==='cashed'){engine&&engine.setStep(0,false);message('Cashed out '+money(round.payoutNano/1e9)+' GRAM','win');haptic('success')}else message('Round updated · choose your next move')}catch(e){message(e.message,'lose');await restore()}finally{setBusy(false)}}
    async function restore(){if(!tgData())return;const version=++requestVersion;try{const data=await api('state');if(version!==requestVersion)return;round=data.round||null;if(round)apply(data);else render();engine&&engine.setStep(round&&round.status==='active'?round.step:0,false);if(round&&round.status==='active')message('Round restored · cross or cash out')}catch(e){message(e.message,'lose')}}
    root.querySelectorAll('[data-cc-risk]').forEach(b=>b.addEventListener('click',()=>{if(busy||round&&round.status==='active')return;mode=b.dataset.ccRisk;render();haptic('light')}));q('[data-cc-half]').addEventListener('click',()=>{input.value=money(Math.max(.001,(Number(input.value)||.1)/2))});q('[data-cc-double]').addEventListener('click',()=>{input.value=money(Math.min(20,(Number(input.value)||.1)*2))});go.addEventListener('click',()=>round&&round.status==='active'?cross():start());cash.addEventListener('click',cashOut);
    function loadImage(src){return new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(Error('Could not load the game assets'));image.src=src})}async function activate(){if(active)return;active=true;const version=++requestVersion;try{if(!engine){const loaded=await Promise.all(['/assets/chicken-cross/road-space-v1.jpeg','/assets/chicken-cross/chicken-v1.png'].map(loadImage));if(!active||version!==requestVersion)return;engine=createRoad(scene,loaded[0],loaded[1]);loading.classList.add('ready');render()}engine.setActive(!document.hidden);await restore()}catch(e){loading.innerHTML='<b>Could not load the game</b><span>Try opening the game again</span>';message('Game assets unavailable','lose')}}function deactivate(){active=false;requestVersion++;if(engine)engine.setActive(false)}window.addEventListener('vexa:view-changed',event=>{if(event.detail&&event.detail.id==='hilo')activate();else deactivate()});document.addEventListener('visibilitychange',()=>{if(engine)engine.setActive(active&&!document.hidden)});if(root.classList.contains('active'))activate();render();
    function createRoad(container,road,chickenAsset){const world=document.createElement('div');world.className='cc-world';const piece=document.createElement('img');piece.className='cc-road-piece';piece.alt='';piece.decoding='async';piece.draggable=false;piece.src=road.src;world.appendChild(piece);const entities=document.createElement('div');entities.className='cc-entities';const chicken=document.createElement('img');chicken.className='cc-chicken';chicken.alt='';chicken.src=chickenAsset.src;entities.appendChild(chicken);world.appendChild(entities);container.appendChild(world);let isActive=false,raf=0,worldWidth=1,shown=0,target=0,moveFrom=0,moveStart=0,moveDone=null,hitStart=0,hitDone=null,last=0;const clamp=(n,min,max)=>Math.max(min,Math.min(max,n)),ease=t=>1-Math.pow(1-t,3),roadStart=.15,roadEnd=.855;function position(step){return step<=0?roadStart-.035:roadStart+(roadEnd-roadStart)*clamp(step,0,8)/8}function renderRoad(){chicken.style.left=(position(shown)*worldWidth).toFixed(2)+'px';chicken.style.top='55%'}function resize(){worldWidth=Math.max(1,container.clientWidth);renderRoad()}const observer=new ResizeObserver(resize);observer.observe(container);resize();function doneMove(){if(moveDone){const done=moveDone;moveDone=null;done()}}function doneHit(){if(hitDone){const done=hitDone;hitDone=null;done()}}function requestFrame(){if(isActive&&!raf){last=performance.now();raf=requestAnimationFrame(frame)}}function frame(now){raf=0;if(!isActive)return;let keep=false;if(moveStart){const t=clamp((now-moveStart)/260,0,1);shown=moveFrom+(target-moveFrom)*ease(t);if(t===1){shown=target;moveStart=0;doneMove()}else keep=true}if(hitStart){const t=clamp((now-hitStart)/260,0,1);container.style.filter='brightness('+(1-.18*Math.sin(t*Math.PI))+')';if(t===1){hitStart=0;container.style.filter='';chicken.classList.remove('hit');doneHit()}else keep=true}renderRoad();if(keep)raf=requestAnimationFrame(frame)}function setActive(value){isActive=value;if(!value){if(raf)cancelAnimationFrame(raf);raf=0;shown=target;renderRoad();doneMove();doneHit()}else{resize();requestFrame()}}function setStep(step){shown=target=clamp(Number(step)||0,0,8);renderRoad();doneMove()}function move(step){moveFrom=shown;target=clamp(Number(step)||0,0,8);chicken.classList.remove('hop');void chicken.offsetWidth;chicken.classList.add('hop');moveStart=performance.now();requestFrame();return new Promise(resolve=>{moveDone=resolve})}function hit(){chicken.classList.remove('hit');void chicken.offsetWidth;chicken.classList.add('hit');hitStart=performance.now();requestFrame();return new Promise(resolve=>{hitDone=resolve})}function confirmStep(step){target=shown=clamp(Number(step)||0,0,8);renderRoad();requestFrame()}return{setActive,setStep,move,hit,confirmStep}}
  })();
  </script>
</section>`;
