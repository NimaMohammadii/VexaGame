export const SLOT_SCRIPT = `
(function(){
  var symbols=[{id:'cherry',label:'Cherry'},{id:'lemon',label:'Lemon'},{id:'orange',label:'Orange'},{id:'grape',label:'Grape'},{id:'watermelon',label:'Watermelon'},{id:'diamond',label:'Diamond'},{id:'gold',label:'Gold Star or Bell'},{id:'lucky7',label:'Lucky 7'}];
  var reelCount=3,symbolHeight=92,virtualCellCount=5,virtualStepMs=52,totalSpinMs=7600,reelStopGapMs=800,soundStopDelayMs=1000,NANO=1000000000;
  var amountNano=NANO,activeCostNano=0,spinning=false,currentIndexes=[0,1,2];
  var slotSound=null,slotAudio=null,slotAudioUrl='',slotSoundTimer=null,slotSoundStopTimer=null,liveTimer=null,liveRows=[],liveRowSeq=0,slotVirtualConfigLoaded=false,slotVirtualProfiles=[],winEffectCleanup=null,winEffectFrame=0;
  var slotCache=window.__VEXA_SLOT_CACHE__||(window.__VEXA_SLOT_CACHE__={frame:null,controls:null,symbols:null,audio:null,images:{},symbolSignature:'',reelsSignature:''});
  function q(id){return document.getElementById(id)}
  function slotActive(){var root=q('slot');return !!(root&&root.classList.contains('active')&&!document.hidden)}
  function fromNano(value){return (Math.max(0,Math.floor(Number(value)||0))/NANO).toFixed(2)}
  function readPointBalance(){return window.VexaTonBalance?Math.max(0,Math.floor(Number(window.VexaTonBalance.read())||0)):0}
  function telegramInitData(){var tg=window.Telegram&&window.Telegram.WebApp;return tg?String(tg.initData||''):''}
  function syncServerBalance(value){var balance=Number(value);if(window.VexaTonBalance&&Number.isFinite(balance)&&balance>=0)window.VexaTonBalance.write(Math.floor(balance),0)}
  function pendingRound(){try{var value=JSON.parse(localStorage.getItem('vexaSlotPendingRound')||'null');return value&&/^slot_[a-zA-Z0-9_-]{16,64}$/.test(value.roundId||'')&&Number(value.amountNano)>0?value:null}catch(e){return null}}
  function savePendingRound(round){try{localStorage.setItem('vexaSlotPendingRound',JSON.stringify(round))}catch(e){}}
  function clearPendingRound(roundId){try{var current=pendingRound();if(!roundId||current&&current.roundId===roundId)localStorage.removeItem('vexaSlotPendingRound')}catch(e){}}
  function newRoundId(){var id='';try{id=window.crypto&&crypto.randomUUID?crypto.randomUUID().replace(/-/g,''):''}catch(e){}return 'slot_'+(id||String(Date.now())+String(Math.random()).slice(2))}
  function requestSlotSpin(stakeNano,roundId,recover){var initData=telegramInitData();if(!initData)return Promise.reject(new Error('Open the Mini App inside Telegram'));var body={initData:initData,roundId:roundId,recover:!!recover};if(!recover)body.amountNano=stakeNano;return fetch('/app/api/slot/spin',{method:'POST',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(body)}).then(function(response){return response.json().catch(function(){return null}).then(function(payload){if(!response.ok){var error=new Error(payload&&payload.error?payload.error:'Spin failed');error.confirmed=true;throw error}return payload})})}
  function awardXP(amount,source,metadata){if(window.VexaLevel&&typeof window.VexaLevel.add==='function')window.VexaLevel.add(amount,source,metadata||{section:'slot'})}
  function setBrand(title){var brand=q('brandTitle');if(brand)brand.textContent=title}
  function setResultText(text){var node=q('slotResultText');if(node)node.textContent=text}
  function ensureMultiplier(){var node=q('slotMultiplier');if(node)return node;var cabinet=document.querySelector('#slot .slot-cabinet');if(!cabinet)return null;node=document.createElement('output');node.id='slotMultiplier';node.className='slot-win-multiplier';node.hidden=true;node.textContent='0.00x';node.setAttribute('aria-live','polite');cabinet.insertBefore(node,cabinet.firstChild);return node}
  function setMultiplierText(value,visible){var node=ensureMultiplier();if(!node)return;node.textContent=Number(value||0).toFixed(2)+'x';node.hidden=!visible}
  function cleanText(value){return String(value==null?'':value).replace(/[&<>"']/g,function(ch){return ch==='&'?'&amp;':ch==='<'?'&lt;':ch==='>'?'&gt;':ch==='"'?'&quot;':'&#39;'})}
  function stableImage(img,url){if(!img||!url)return;if(img.dataset.slotStableSrc===url&&img.getAttribute('src')){img.classList.add('is-loaded');return}img.dataset.slotStableSrc=url;img.loading='eager';img.decoding='async';img.onload=function(){img.classList.add('is-loaded')};img.onerror=function(){img.classList.remove('is-loaded')};img.src=url}
  function cachedJson(key,url){if(slotCache[key])return Promise.resolve(slotCache[key]);return fetch(url,{cache:'force-cache'}).then(function(response){return response.json().then(function(body){var result={ok:response.ok,body:body};if(response.ok)slotCache[key]=result;return result})})}
  function symbolPayloadSignature(nextSymbols){return nextSymbols.map(function(symbol){return [symbol.id||'',symbol.label||'',symbol.imageUrl||''].join('|')}).join('||')}
  function applySymbolsPayload(body){if(!body||!body.symbols)return false;var byId={};body.symbols.forEach(function(symbol){byId[symbol.id]=symbol});var next=symbols.map(function(symbol){var uploaded=byId[symbol.id];if(!uploaded||!uploaded.imageUrl)return{id:symbol.id,label:symbol.label};return{id:symbol.id,label:uploaded.label||symbol.label,imageUrl:uploaded.imageUrl}});var signature=symbolPayloadSignature(next);if(slotCache.symbolSignature===signature)return false;symbols=next;slotCache.symbolSignature=signature;return true}
  function currentUserName(){try{var user=window.Telegram&&window.Telegram.WebApp&&window.Telegram.WebApp.initDataUnsafe&&window.Telegram.WebApp.initDataUnsafe.user;if(user){return String([user.first_name,user.last_name].filter(Boolean).join(' ')).replace(/[<>]/g,'').trim().slice(0,18)}}catch(e){}return localStorage.getItem('slotLiveName')||'You'}
  function symbolFallback(symbol){var icons={cherry:'🍒',lemon:'🍋',orange:'🍊',grape:'🍇',watermelon:'🍉',diamond:'💎',gold:'⭐',lucky7:'7️⃣'};return symbol?(icons[symbol.id]||symbol.label||symbol.id):'—'}
  function randomSymbolIndex(){if(window.crypto&&window.crypto.getRandomValues){var values=new Uint32Array(1);window.crypto.getRandomValues(values);return values[0]%symbols.length}return Math.floor(Math.random()*symbols.length)}
  function randomInt(max){if(window.crypto&&window.crypto.getRandomValues){var values=new Uint32Array(1);window.crypto.getRandomValues(values);return values[0]%max}return Math.floor(Math.random()*max)}
  function randomResult(){var out=[];for(var i=0;i<reelCount;i++)out.push(randomSymbolIndex());return out}
  function slotLiveResultHtml(result){return result.map(function(symbolIndex){var symbol=symbols[symbolIndex];if(symbol&&symbol.imageUrl)return '<span class="slot-live-symbol has-image"><img src="'+cleanText(symbol.imageUrl)+'" alt="'+cleanText(symbol.label||symbol.id)+'" loading="eager" decoding="async"><span>'+cleanText(symbolFallback(symbol))+'</span></span>';return '<span class="slot-live-symbol">'+cleanText(symbolFallback(symbol))+'</span>'}).join('')}
  function slotLiveRowKey(row,index){return cleanText((row.name||'')+':'+(row.result||[]).join('-')+':'+index)}
  function normalizeLiveResult(result){var out=Array.isArray(result)?result.map(function(v){var n=Math.floor(Number(v));return Number.isFinite(n)?Math.max(0,Math.min(symbols.length-1,n)):0}).slice(0,reelCount):[];while(out.length<reelCount)out.push(randomSymbolIndex());return out}
  function randomProfileResult(profile){var results=profile&&Array.isArray(profile.results)?profile.results:[];if(!results.length)return randomResult();return normalizeLiveResult(results[randomInt(results.length)])}
  function loadSlotVirtualConfig(){if(slotVirtualConfigLoaded||typeof fetch!=='function')return;slotVirtualConfigLoaded=true;fetch('/app/api/slot-virtual-users',{cache:'no-store'}).then(function(r){return r.ok?r.json():null}).then(function(data){if(!data||!Array.isArray(data.users)||!data.users.length)return;slotVirtualProfiles=data.users.map(function(user){return {name:String(user.name||'Player'),results:Array.isArray(user.results)?user.results:[]}}).filter(function(user){return user.name&&user.results.length});if(slotVirtualProfiles.length){liveRows=[];renderLive()}}).catch(function(){})}
  function defaultSlotVirtualProfiles(){return [{name:'AriSpin',results:[[0,0,0],[1,1,0],[5,5,5]]},{name:'MayaWin',results:[[1,1,1],[2,2,4],[6,6,6]]},{name:'NimaLuck',results:[[2,2,2],[3,3,1],[7,7,7]]},{name:'NoraLux',results:[[3,3,3],[4,4,0],[5,5,5]]},{name:'KianRush',results:[[4,4,4],[0,0,2],[6,6,6]]},{name:'ParsaKing',results:[[7,7,7],[5,5,5],[1,1,3]]}]}
  function liveProfiles(){return slotVirtualProfiles.length?slotVirtualProfiles:defaultSlotVirtualProfiles()}
  function makeVirtualLiveRow(prefix){var profiles=liveProfiles();var profile=profiles[randomInt(profiles.length)];return {key:prefix+'-'+(++liveRowSeq),name:profile.name,result:randomProfileResult(profile)}}
  function renderLive(){var list=q('slotLiveList');if(!list)return;if(!liveRows.length){liveRows=[makeVirtualLiveRow('seed'),makeVirtualLiveRow('seed'),makeVirtualLiveRow('seed')]}var rows=liveRows.slice(0,10);var existing={};Array.prototype.slice.call(list.children).forEach(function(node){var key=node&&node.dataset&&node.dataset.liveRowKey;if(key)existing[key]=node});var fragment=document.createDocumentFragment();rows.forEach(function(row,index){if(!row.key)row.key='live-'+(++liveRowSeq);var key=String(row.key);var node=existing[key];if(!node){node=document.createElement('div');node.className='slot-live-row is-entering';node.dataset.liveRowKey=key;node.innerHTML='<span class="slot-live-user">'+cleanText(row.name)+'</span><span class="slot-live-result">'+slotLiveResultHtml(row.result)+'</span>'}fragment.appendChild(node)});list.replaceChildren(fragment)}
  function pushSlotLiveUserResult(indexes){liveRows=liveRows.filter(function(row){return row.name!==currentUserName()});liveRows.unshift({key:'user-'+currentUserName()+'-'+(++liveRowSeq),name:currentUserName(),result:indexes});renderLive()}
  function bindSlotLive(){loadSlotVirtualConfig();renderLive();var box=q('slotLive'),toggle=q('slotLiveToggle');if(toggle&&box){toggle.onclick=function(){var open=!box.classList.contains('open');box.classList.toggle('open',open);toggle.setAttribute('aria-expanded',open?'true':'false')}}function stopLiveLoop(){if(liveTimer){clearTimeout(liveTimer);liveTimer=null}}function scheduleLiveLoop(delay){stopLiveLoop();if(!slotActive())return;liveTimer=setTimeout(function(){liveTimer=null;if(!slotActive())return;liveRows.unshift(makeVirtualLiveRow('auto'));liveRows=liveRows.slice(0,10);renderLive();scheduleLiveLoop(2200)},Math.max(250,delay||2200))}scheduleLiveLoop(2200)}
  function betValue(){var input=q('slotAmount');var value=Math.floor(Number(input&&input.value)||1);return Math.max(1,Math.min(999,value))}
  function maxBetValue(){return Math.max(1,Math.min(999,Math.floor(readPointBalance()/NANO)||1))}
  function readAmountInput(){var value=betValue();var input=q('slotAmount');if(input)input.value=String(value);amountNano=value*NANO;return amountNano}
  function syncAmountInput(){var input=q('slotAmount');if(input&&!input.value)input.value='1';readAmountInput()}
  function setBetValue(value){var input=q('slotAmount');if(!input||spinning)return;input.value=String(Math.max(1,Math.min(999,Math.floor(Number(value)||1))));readAmountInput();refreshControls()}
  function promptSlotBetValue(){if(spinning)return;var current=String(betValue()),next=null;try{next=window.prompt('Enter TON amount',current)}catch(e){return}if(next===null)return;var parsed=Math.floor(Number(String(next).trim().replace(',','.')));if(!Number.isFinite(parsed))return;setBetValue(parsed)}
  function refreshControls(){var button=q('slotSpinButton'),input=q('slotAmount'),minus=q('slotBetMinus'),plus=q('slotBetPlus'),half=q('slotBetHalf'),max=q('slotBetMax');var value=Math.max(1,Math.floor(readAmountInput()/NANO));if(input)input.disabled=spinning;if(minus)minus.disabled=spinning||value<=1;if(plus)plus.disabled=spinning||value>=999;if(half)half.disabled=spinning||value<=1;if(max)max.disabled=spinning;var label=button&&button.querySelector('.slot-spin-label');var cost=button&&button.querySelector('.slot-spin-cost');if(button){button.disabled=spinning;button.setAttribute('aria-label','Spin '+value+' TON')}if(label)label.textContent=spinning?'RUNNING':'SPIN';if(cost)cost.textContent=value+' TON'}
  function prepareSlotSound(url){slotAudioUrl=url||'';slotAudio=null;if(!slotAudioUrl)return;slotAudio=new Audio(slotAudioUrl);slotAudio.loop=false;slotAudio.preload='auto';try{slotAudio.load()}catch(e){}}
  function startSlotSound(){if(!slotAudioUrl)return;stopSlotSound();slotSoundTimer=setTimeout(function(){var audio=slotAudio||new Audio(slotAudioUrl);audio.loop=false;audio.preload='auto';audio.currentTime=0;slotSound=audio;audio.play().catch(function(){})},120)}
  function scheduleSlotSoundStop(){if(slotSoundStopTimer){clearTimeout(slotSoundStopTimer);slotSoundStopTimer=null}slotSoundStopTimer=setTimeout(stopSlotSound,soundStopDelayMs)}
  function stopSlotSound(){if(slotSoundTimer){clearTimeout(slotSoundTimer);slotSoundTimer=null}if(slotSoundStopTimer){clearTimeout(slotSoundStopTimer);slotSoundStopTimer=null}if(!slotSound)return;try{slotSound.pause();slotSound.currentTime=0}catch(e){}slotSound=null}
  function createSymbol(symbol){var cell=document.createElement('div');cell.className='slot-symbol';var fallback=document.createElement('span');fallback.className='slot-symbol-fallback';fallback.textContent=symbolFallback(symbol);cell.appendChild(fallback);if(symbol&&symbol.imageUrl){var expectedUrl=symbol.imageUrl;var img=document.createElement('img');img.className='slot-symbol-image';img.alt=symbol.label||symbol.id||'Slot symbol';img.decoding='async';img.draggable=false;img.loading='eager';img.style.display='block';cell.classList.add('has-image');img.onload=function(){if(img.getAttribute('src')===expectedUrl){img.style.display='block';cell.classList.add('has-image')}};img.onerror=function(){img.style.display='none'};img.src=expectedUrl;cell.appendChild(img)}return cell}
  function symbolImageUrls(){var seen={},urls=[];symbols.forEach(function(symbol){var url=symbol&&symbol.imageUrl;if(url&&!seen[url]){seen[url]=1;urls.push(url)}});return urls}
  function preloadImage(url){if(!url)return Promise.resolve();if(slotCache.images[url]&&slotCache.images[url].ready)return slotCache.images[url].ready;var img=slotCache.images[url]&&slotCache.images[url].img;if(!img){img=new Image();img.decoding='async';img.loading='eager';slotCache.images[url]={img:img,ready:null}}var ready=new Promise(function(resolve){if(img.complete&&img.naturalWidth){resolve();return}img.onload=function(){resolve()};img.onerror=function(){resolve()};if(img.getAttribute('src')!==url)img.src=url}).then(function(){if(img.decode)return img.decode().catch(function(){})});slotCache.images[url].ready=ready;return ready}
  function preloadSymbolImages(){return Promise.all(symbolImageUrls().map(preloadImage))}
  function modulo(value,size){return((value%size)+size)%size}
  function stripNode(reelIndex){return document.querySelector('[data-slot-reel="'+reelIndex+'"] .slot-reel-strip')}
  function symbolStep(){var sample=document.querySelector('.slot-symbol');return sample&&sample.offsetHeight?sample.offsetHeight:symbolHeight}
  function paintSymbolCell(cell,symbol){if(!cell)return;var fallback=cell.querySelector('.slot-symbol-fallback');if(!fallback){fallback=document.createElement('span');fallback.className='slot-symbol-fallback';cell.appendChild(fallback)}fallback.textContent=symbolFallback(symbol);var img=cell.querySelector('.slot-symbol-image');if(symbol&&symbol.imageUrl){var expectedUrl=symbol.imageUrl;if(!img){img=document.createElement('img');img.className='slot-symbol-image';img.decoding='async';img.draggable=false;img.loading='eager';cell.appendChild(img)}var previousUrl=img.getAttribute('src')||'';cell.classList.add('has-image');img.style.display='block';img.alt=symbol.label||symbol.id||'Slot symbol';img.onload=function(){if(img.getAttribute('src')===expectedUrl){img.style.display='block';cell.classList.add('has-image')}};img.onerror=function(){if(previousUrl&&previousUrl!==expectedUrl){img.onerror=null;img.src=previousUrl}img.style.display='block';cell.classList.add('has-image')};if(img.getAttribute('src')!==expectedUrl)img.src=expectedUrl}else{cell.classList.remove('has-image');if(img)img.remove()}}
  function markReelCurve(strip,index){if(!strip)return;Array.prototype.forEach.call(strip.children,function(cell){cell.classList.remove('is-reel-top','is-reel-center','is-reel-bottom')});var top=strip.children[index-1],center=strip.children[index],bottom=strip.children[index+1];if(top)top.classList.add('is-reel-top');if(center)center.classList.add('is-reel-center');if(bottom)bottom.classList.add('is-reel-bottom')}
  function buildStrip(reelIndex){var strip=stripNode(reelIndex);if(!strip)return;var count=Math.max(1,symbols.length);var center=modulo(currentIndexes[reelIndex]||0,count);var fragment=document.createDocumentFragment();for(var i=0;i<virtualCellCount;i++)fragment.appendChild(createSymbol(symbols[modulo(center+i-2,count)]));strip.replaceChildren(fragment);strip.dataset.slotCenter=String(center);strip.dataset.slotStripSignature=slotCache.symbolSignature||'fallback';strip.style.transition='none';strip.style.transform='translate3d(0,'+(-symbolStep())+'px,0)';markReelCurve(strip,2)}
  function setReelPosition(reelIndex,symbolIndex,animate){var strip=stripNode(reelIndex);if(!strip)return;var count=Math.max(1,symbols.length);var center=modulo(symbolIndex,count);if(strip.children.length!==virtualCellCount){currentIndexes[reelIndex]=center;buildStrip(reelIndex);return}Array.prototype.forEach.call(strip.children,function(cell,i){paintSymbolCell(cell,symbols[modulo(center+i-2,count)])});strip.dataset.slotCenter=String(center);strip.style.transition='none';strip.style.transform='translate3d(0,'+(-symbolStep())+'px,0)';markReelCurve(strip,2)}
  function initReels(){for(var i=0;i<reelCount;i++){buildStrip(i);setReelPosition(i,currentIndexes[i],false)}}
  function refreshReels(){if(spinning){slotCache.pendingReelRefresh=true;return}var signature=slotCache.symbolSignature||'fallback';if(slotCache.reelsSignature===signature){var ready=true;for(var r=0;r<reelCount;r++){var strip=stripNode(r);if(!strip||strip.dataset.slotStripSignature!==signature||strip.children.length!==virtualCellCount)ready=false}if(ready)return}for(var i=0;i<reelCount;i++){buildStrip(i);setReelPosition(i,currentIndexes[i],false)}slotCache.reelsSignature=signature;slotCache.pendingReelRefresh=false}
  function runVirtualReel(reelIndex,targetSymbolIndex,onDone){var strip=stripNode(reelIndex);if(!strip){if(onDone)onDone();return}var count=Math.max(1,symbols.length);var center=Number(strip.dataset.slotCenter);if(!Number.isFinite(center))center=modulo(currentIndexes[reelIndex]||0,count);var target=modulo(targetSymbolIndex,count);var delta=modulo(target-center,count);var targetDuration=totalSpinMs-((reelCount-reelIndex-1)*reelStopGapMs);var minimumSteps=Math.max(count,Math.floor(targetDuration/(virtualStepMs+4)));var totalSteps=minimumSteps+modulo(delta-modulo(minimumSteps,count),count);var completed=0;var step=symbolStep();var schedule=window.requestAnimationFrame||function(callback){return setTimeout(function(){callback(Date.now())},16)};var now=function(timestamp){if(Number.isFinite(timestamp))return timestamp;return window.performance&&typeof window.performance.now==='function'?window.performance.now():Date.now()};var startedAt=0;Array.prototype.forEach.call(strip.children,function(cell){cell.classList.remove('is-reel-top','is-reel-center','is-reel-bottom');cell.style.setProperty('transition','none','important')});strip.style.transition='none';strip.style.transform='translate3d(0,'+(-step)+'px,0)';strip.style.willChange='transform';function recycleOne(){center=modulo(center+1,count);var moved=strip.firstElementChild;if(moved){strip.appendChild(moved);paintSymbolCell(moved,symbols[modulo(center+2,count)])}strip.dataset.slotCenter=String(center)}function finishVirtualReel(){strip.style.transition='none';strip.style.transform='translate3d(0,'+(-step)+'px,0)';strip.style.willChange='auto';currentIndexes[reelIndex]=target;setReelPosition(reelIndex,target,false);Array.prototype.forEach.call(strip.children,function(cell){cell.style.removeProperty('transition')});if(onDone)onDone()}function frame(timestamp){if(!slotActive()){finishVirtualReel();return}var frameNow=now(timestamp);if(!startedAt)startedAt=frameNow;var elapsed=Math.min(targetDuration,Math.max(0,frameNow-startedAt));var linear=targetDuration>0?elapsed/targetDuration:1;var progress=(linear*linear*(3-(2*linear)))*totalSteps;var whole=Math.min(totalSteps,Math.floor(progress));while(completed<whole){recycleOne();completed++}if(elapsed>=targetDuration||completed>=totalSteps){finishVirtualReel();return}var fraction=progress-whole;strip.style.transform='translate3d(0,'+(-step*(1+fraction))+'px,0)';schedule(frame)}schedule(frame)}
  function isFruitSymbol(symbolId){return ['cherry','lemon','orange','grape','watermelon'].indexOf(symbolId)!==-1}
  function resultCounts(result){var counts={};result.forEach(function(symbolIndex){var symbol=symbols[symbolIndex];var id=symbol&&symbol.id;if(id)counts[id]=(counts[id]||0)+1});return counts}
  function topResultEntry(counts){var top={symbolId:'',count:0};Object.keys(counts).forEach(function(id){if(counts[id]>top.count)top={symbolId:id,count:counts[id]}});return top}
  function resultProfile(result){var entry=topResultEntry(resultCounts(result));if(entry.count===3){if(isFruitSymbol(entry.symbolId))return{tier:'triple-fruit',multiplier:5,xp:30};if(entry.symbolId==='diamond')return{tier:'triple-diamond',multiplier:15,xp:80};if(entry.symbolId==='gold')return{tier:'triple-gold',multiplier:30,xp:150};if(entry.symbolId==='lucky7')return{tier:'triple-seven',multiplier:200,xp:300}}if(entry.count===2&&isFruitSymbol(entry.symbolId))return{tier:'pair-fruit',multiplier:.8,xp:10};return{tier:'standard',multiplier:0,xp:5}}
  function resultLabel(profile,deltaNano){if(deltaNano>0)return'Result +'+fromNano(deltaNano);return'Result 0.00'}
  function clearWinEffect(){if(winEffectFrame){var cancel=window.cancelAnimationFrame||clearTimeout;cancel(winEffectFrame);winEffectFrame=0}if(winEffectCleanup)winEffectCleanup()}
  function scheduleWinEffect(result,profile,resultNano){clearWinEffect();var frame=window.requestAnimationFrame||function(callback){return setTimeout(callback,16)};winEffectFrame=frame(function(){winEffectFrame=frame(function(){winEffectFrame=0;if(!spinning)showWinEffect(result,profile,resultNano)})})}
  function showWinEffect(result,profile,resultNano){clearWinEffect();var cleanup=null;try{var layer=document.createElement('div');layer.className='slot-win-pop-layer';layer.style.cssText='position:fixed;inset:0;z-index:2147483000;pointer-events:none;overflow:hidden;';var centerX=window.innerWidth/2;var centerY=Math.max(150,window.innerHeight*.42);var glow=document.createElement('div');glow.style.cssText='position:absolute;left:'+centerX+'px;top:'+centerY+'px;width:330px;height:330px;border-radius:50%;background:radial-gradient(circle,rgba(255,232,150,.58),rgba(214,38,82,.32) 42%,rgba(255,255,255,.08) 58%,rgba(0,0,0,0) 74%);filter:blur(18px);opacity:0;transform:translate(-50%,-50%) scale(.55);';layer.appendChild(glow);document.body.appendChild(layer);var finals=[centerX-104,centerX,centerX+104],hiddenCells=[],returnAnimations=[],finished=false;cleanup=function(){if(finished)return;finished=true;hiddenCells.forEach(function(entry){if(entry.value)entry.node.style.setProperty('visibility',entry.value,entry.priority);else entry.node.style.removeProperty('visibility')});hiddenCells=[];if(layer.parentNode)layer.parentNode.removeChild(layer);if(winEffectCleanup===cleanup)winEffectCleanup=null};winEffectCleanup=cleanup;result.forEach(function(symbolIndex,i){var sourceCell=document.querySelector('#slot [data-slot-reel="'+i+'"] .slot-symbol.is-reel-center');var source=sourceCell&&(sourceCell.querySelector('.slot-symbol-image')||sourceCell.querySelector('.slot-symbol-fallback'));if(!source)return;var rect=source.getBoundingClientRect();var computed=window.getComputedStyle?getComputedStyle(source):null;var baseFilter=computed&&computed.filter&&computed.filter!=='none'?computed.filter:'none';var item=source.cloneNode(true);if(item.tagName==='IMG'&&source.currentSrc)item.src=source.currentSrc;hiddenCells.push({node:sourceCell,value:sourceCell.style.getPropertyValue('visibility'),priority:sourceCell.style.getPropertyPriority('visibility')});sourceCell.style.setProperty('visibility','hidden','important');item.removeAttribute('id');item.setAttribute('aria-hidden','true');item.style.cssText='position:absolute;left:'+(rect.left+rect.width/2)+'px;top:'+(rect.top+rect.height/2)+'px;width:'+rect.width+'px;height:'+rect.height+'px;margin:0;padding:0;display:block;object-fit:'+(computed&&computed.objectFit||'contain')+';visibility:visible;opacity:1;background:transparent;border:0;border-radius:0;outline:0;box-shadow:none;transform:translate(-50%,-50%) scale(1);filter:'+baseFilter+';will-change:left,top,transform,opacity,filter;';if(item.tagName!=='IMG'){item.style.fontSize=computed&&computed.fontSize||'38px';item.style.lineHeight='1';item.style.textAlign='center';item.style.color=computed&&computed.color||'#fff';item.style.textShadow=computed&&computed.textShadow||'0 2px 18px rgba(0,0,0,.78)'}layer.appendChild(item);var startX=rect.left+rect.width/2,startY=rect.top+rect.height/2,bendX=finals[i]+((startX-finals[i])*.46),bendY=Math.min(centerY,startY)-28,nearX=startX+((finals[i]-startX)*.055),nearY=startY-6,turn=(i-1)*7;returnAnimations.push(item.animate([{offset:0,left:startX+'px',top:startY+'px',transform:'translate(-50%,-50%) scale(1) rotate(0deg)',opacity:1,filter:baseFilter},{offset:.10,left:startX+'px',top:startY+'px',transform:'translate(-50%,-50%) scale(1.08) rotate(0deg)',opacity:1,filter:baseFilter},{offset:.28,left:finals[i]+'px',top:centerY+'px',transform:'translate(-50%,-50%) scale(1.68) rotate(0deg)',opacity:1,filter:baseFilter},{offset:.77,left:finals[i]+'px',top:centerY+'px',transform:'translate(-50%,-50%) scale(1.58) rotate(0deg)',opacity:1,filter:baseFilter},{offset:.83,left:finals[i]+'px',top:(centerY-7)+'px',transform:'translate(-50%,-50%) scale(1.66) rotate('+(-turn*.35)+'deg)',opacity:1,filter:baseFilter},{offset:.91,left:bendX+'px',top:bendY+'px',transform:'translate(-50%,-50%) scale(1.30) rotate('+turn+'deg)',opacity:1,filter:baseFilter},{offset:.975,left:nearX+'px',top:nearY+'px',transform:'translate(-50%,-50%) scale(.94) rotate('+(-turn*.22)+'deg)',opacity:1,filter:baseFilter},{offset:1,left:startX+'px',top:startY+'px',transform:'translate(-50%,-50%) scale(1) rotate(0deg)',opacity:1,filter:baseFilter}],{duration:7800,delay:i*120,easing:'cubic-bezier(.16,.84,.22,1)',fill:'both'}))});glow.animate([{offset:0,opacity:0,transform:'translate(-50%,-50%) scale(.48)'},{offset:.18,opacity:1,transform:'translate(-50%,-50%) scale(1)'},{offset:.74,opacity:1,transform:'translate(-50%,-50%) scale(1.08)'},{offset:.88,opacity:.38,transform:'translate(-50%,-50%) scale(1.18)'},{offset:1,opacity:0,transform:'translate(-50%,-50%) scale(1.28)'}],{duration:7800,easing:'cubic-bezier(.18,.84,.22,1)',fill:'both'});if(returnAnimations.length)returnAnimations[returnAnimations.length-1].onfinish=cleanup;else cleanup()}catch(e){if(cleanup)cleanup()}}
  function finish(result,settlement){var button=q('slotSpinButton');var box=document.querySelector('.slot-machine');var profile=resultProfile(result);var serverMultiplier=Math.max(0,Number(settlement&&settlement.multiplier)||0);var active=serverMultiplier>0;var resultNano=Math.max(0,Math.floor(Number(settlement&&settlement.payoutNano)||0));clearPendingRound(settlement&&settlement.roundId);spinning=false;scheduleSlotSoundStop();syncServerBalance(settlement&&settlement.tonBalanceNano);setMultiplierText(serverMultiplier,active);setResultText(resultLabel(profile,resultNano));pushSlotLiveUserResult(result);if(slotCache.pendingReelRefresh)refreshReels();if(button)button.disabled=false;if(box){box.classList.remove('is-spinning');box.classList.toggle('is-win',active)}awardXP(profile.xp,'reel-result',{section:'slot',event:'finish',tier:String(settlement&&settlement.tier||profile.tier),multiplier:serverMultiplier});activeCostNano=0;refreshControls();if(active&&slotActive())scheduleWinEffect(result,profile,resultNano)}
  function failSpin(error){if(error&&error.confirmed)clearPendingRound();var button=q('slotSpinButton');var box=document.querySelector('.slot-machine');spinning=false;activeCostNano=0;stopSlotSound();setMultiplierText(0,false);setResultText(error&&error.message?error.message:'Spin failed');if(button)button.disabled=false;if(box)box.classList.remove('is-spinning');refreshControls()}
  function showSettlement(settlement,animate){var result=normalizeLiveResult(settlement&&settlement.result);if(!settlement||!Array.isArray(settlement.result)||settlement.result.length!==reelCount)throw new Error('Invalid Slot result');if(!animate){finish(result,settlement);return}var pending=reelCount;result.forEach(function(symbolIndex,reelIndex){runVirtualReel(reelIndex,symbolIndex,function(){pending--;if(pending<=0)finish(result,settlement)})})}
  function recoverPendingRound(){var pending=pendingRound();if(!pending)return;requestSlotSpin(0,pending.roundId,true).then(function(settlement){showSettlement(settlement,false)}).catch(function(error){if(error&&error.confirmed)clearPendingRound()})}
  function spin(){if(spinning)return;var pending=pendingRound();if(pending){spinning=true;refreshControls();requestSlotSpin(0,pending.roundId,true).then(function(settlement){showSettlement(settlement,true)}).catch(failSpin);return}clearWinEffect();var button=q('slotSpinButton');var box=document.querySelector('.slot-machine');readAmountInput();activeCostNano=amountNano;if(readPointBalance()<activeCostNano){setResultText('Not enough TON');return}var roundId=newRoundId();savePendingRound({roundId:roundId,amountNano:activeCostNano});spinning=true;awardXP(2,'reel-start',{section:'slot',event:'spin',amountNano:activeCostNano});setResultText('Running '+String(Math.max(1,Math.floor(activeCostNano/NANO)))+' TON');setMultiplierText(0,false);startSlotSound();if(button)button.disabled=true;if(box){box.classList.remove('is-win');box.classList.add('is-spinning')}refreshControls();requestSlotSpin(activeCostNano,roundId,false).then(function(settlement){showSettlement(settlement,true)}).catch(failSpin)}
  function loadSlotSymbols(){if(slotCache.symbols&&slotCache.symbols.body){var changed=applySymbolsPayload(slotCache.symbols.body);preloadSymbolImages().then(function(){if(changed)refreshReels();renderLive()});return}cachedJson('symbols','/app/api/slot-symbols').then(function(result){if(!result.ok||!result.body)return;var changed=applySymbolsPayload(result.body);preloadSymbolImages().then(function(){if(changed)refreshReels();renderLive()})}).catch(function(){})}
  function loadSlotFrame(){var img=q('slotFrameImage');if(!img||img.dataset.slotGenerated==='true')return;if(slotCache.frame&&slotCache.frame.body&&slotCache.frame.body.slotFrameUrl){stableImage(img,slotCache.frame.body.slotFrameUrl);return}cachedJson('frame','/app/api/slot-frame').then(function(result){if(!result.ok||!result.body||!result.body.slotFrameUrl)return;stableImage(img,result.body.slotFrameUrl)}).catch(function(){img.classList.remove('is-loaded')})}
  function loadSlotSpinAudio(){if(slotCache.audio&&slotCache.audio.body&&slotCache.audio.body.audioUrl){prepareSlotSound(slotCache.audio.body.audioUrl);return}cachedJson('audio','/app/api/slot-spin-audio').then(function(result){if(!result.ok||!result.body||!result.body.audioUrl)return;prepareSlotSound(result.body.audioUrl)}).catch(function(){})}
  function smartSlotLiveSync(){if(slotActive()){if(!liveTimer){liveRows.unshift(makeVirtualLiveRow('auto'));liveRows=liveRows.slice(0,10);renderLive();bindSlotLive()}}else{if(liveTimer){clearTimeout(liveTimer);liveTimer=null}var live=q('slotLive');if(live&&live._slotCloseDrawer)live._slotCloseDrawer();stopSlotSound();clearWinEffect()}}
  function setupSlotLiveDrawer(){var box=q('slotLive'),root=q('slot');if(!box||!root||box.dataset.slotDrawerBound==='1')return;box.dataset.slotDrawerBound='1';var trigger=document.createElement('button'),backdrop=document.createElement('div'),head=box.querySelector('.slot-live-head'),toggle=q('slotLiveToggle');trigger.id='slotLiveMenuButton';trigger.className='slot-live-menu-button';trigger.type='button';trigger.textContent='Live Bet';trigger.setAttribute('aria-expanded','false');backdrop.id='slotLiveDrawerBackdrop';backdrop.className='slot-live-drawer-backdrop';box.classList.add('slot-live-drawer');if(head){head.classList.add('slot-live-drawer-head');var title=head.querySelector('.slot-live-title');if(title)title.textContent='Live Bet'}if(box.parentNode!==root)root.appendChild(box);root.appendChild(backdrop);root.insertBefore(trigger,root.firstChild);function setDrawer(open){var visible=!!open;box.classList.toggle('open',visible);backdrop.classList.toggle('is-open',visible);root.classList.toggle('slot-live-drawer-open',visible);trigger.setAttribute('aria-expanded',visible?'true':'false')}trigger.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();setDrawer(!box.classList.contains('open'))},true);backdrop.addEventListener('click',function(){setDrawer(false)});if(toggle)toggle.onclick=function(){setDrawer(false)};box._slotCloseDrawer=function(){setDrawer(false)}}
  function bind(){if(slotCache.symbols&&slotCache.symbols.body)applySymbolsPayload(slotCache.symbols.body);initReels();preloadSymbolImages().then(refreshReels);loadSlotFrame();loadSlotSymbols();loadSlotSpinAudio();syncAmountInput();setMultiplierText(0,false);setResultText('Spin '+String(betValue())+' TON');refreshControls();recoverPendingRound();bindSlotLive();setupSlotLiveDrawer();var spinButton=q('slotSpinButton'),minus=q('slotBetMinus'),plus=q('slotBetPlus'),half=q('slotBetHalf'),max=q('slotBetMax'),amount=q('slotAmount');if(spinButton&&!spinButton.dataset.slotSpinBound){spinButton.dataset.slotSpinBound='1';spinButton.addEventListener('click',spin)}if(minus&&!minus.dataset.slotBetBound){minus.dataset.slotBetBound='1';minus.addEventListener('click',function(){setBetValue(betValue()-1)})}if(plus&&!plus.dataset.slotBetBound){plus.dataset.slotBetBound='1';plus.addEventListener('click',function(){setBetValue(betValue()+1)})}if(half&&!half.dataset.slotBetBound){half.dataset.slotBetBound='1';half.addEventListener('click',function(){setBetValue(Math.max(1,Math.floor(betValue()/2)))})}if(max&&!max.dataset.slotBetBound){max.dataset.slotBetBound='1';max.addEventListener('click',function(){setBetValue(maxBetValue())})}if(amount&&!amount.dataset.slotBetBound){amount.dataset.slotBetBound='1';amount.readOnly=true;amount.setAttribute('aria-haspopup','dialog');amount.addEventListener('click',function(ev){if(spinning)return;ev.preventDefault();try{amount.blur()}catch(e){}promptSlotBetValue()})}if(!slotCache.openBrandBound){slotCache.openBrandBound=true;document.addEventListener('click',function(ev){var openButton=ev.target&&ev.target.closest?ev.target.closest('[data-game-view="slot"]'):null;if(openButton){setBrand('Slot');setTimeout(smartSlotLiveSync,120)}},true)}}
  document.addEventListener('visibilitychange',function(){setTimeout(smartSlotLiveSync,80)});
  window.addEventListener('focus',smartSlotLiveSync);
  window.addEventListener('vexa:view-changed',smartSlotLiveSync);
  if(window.MutationObserver){var slotRoot=q('slot');if(slotRoot)new MutationObserver(smartSlotLiveSync).observe(slotRoot,{attributes:true,attributeFilter:['class']})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();
`;
